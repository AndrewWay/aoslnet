// Pretty text
// Take the names found in the json objects and clean them up to be proper data labels in the display

function UpperCaseFirst(string){
  return string.charAt(0).toUpperCase()+string.slice(1);
}
